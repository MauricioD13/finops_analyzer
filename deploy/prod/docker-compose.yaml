---
services:
  finops-analyzer:
    env_file:
      - ../../.env_vars
    build:
      context: ../..
      dockerfile: deploy/prod/dockerfile.finops_analyzer.yaml
    container_name: finops-analyzer
    ports:
      - "8000:8000"
    volumes:
      # Persist input/output directories for file processing
      - finops-input:/app/files/uploads
      - finops-output:/app/files/downloads
      - finops-logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
      - LOG_LEVEL=INFO
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('http://localhost:8000/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  finops-input:
    driver: local
  finops-output:
    driver: local
  finops-logs:
    driver: local
